# Harper DX Lab — Docker environment
# Copy to .env and adjust as needed:  cp .env.example .env

# Harper admin credentials for isolated worker containers.
# These are only used inside ephemeral Docker stacks — never exposed externally.
HDB_ADMIN_USERNAME=admin
HDB_ADMIN_PASSWORD=password
